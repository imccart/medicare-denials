# _build-all.R — Master data build script
# Run from project root: source("code/data-build/_build-all.R")

pacman::p_load(tidyverse)

# Add `source("code/data-build/1-foo.R")` lines here as numbered sub-scripts are created.
